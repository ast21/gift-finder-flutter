import 'package:flutter/material.dart';

class Finder extends StatelessWidget {
  const Finder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('This Finder')),
    );
  }
}
